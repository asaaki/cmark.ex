#!/usr/bin/env elixir
Code.prepend_path("_build/dev/lib/cmark/ebin")

defmodule Cmark.SpecRunner do
  def run do
    IO.read(:erlang.group_leader(), :all)
      |> Cmark.to_html
      |> IO.write
  end
end

Cmark.SpecRunner.run
